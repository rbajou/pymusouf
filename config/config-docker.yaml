default_survey: soufriere

paths:
  # Configuration pour Docker : chemins internes au conteneur
  # Ces chemins doivent correspondre aux VOLUME définis dans docker-compose.yml
  data: /data
  structure: /struct
  sample: ./sample

runtime:
  use_sample_data: false
  create_missing_dirs: true
